apiVersion: v1
kind: Service
metadata:
  name: audit-api
  labels:
    app.kubernetes.io/name: audit
    app.kubernetes.io/component: api
    app.kubernetes.io/managed-by: kustomize
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: audit
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
