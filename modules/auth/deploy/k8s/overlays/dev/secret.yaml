# dev placeholder. NOT a real secret — values are equivalent to compose.yml.
# For staging/prod, replace with secretGenerator from a gitignored .env or
# an ExternalSecret CRD pointing at a real Secret Store.
apiVersion: v1
kind: Secret
metadata:
  name: auth-api-secret
type: Opaque
stringData:
  DB_URL: postgres://auth:auth@auth-db.auth.svc.cluster.local:5432/auth?sslmode=disable
